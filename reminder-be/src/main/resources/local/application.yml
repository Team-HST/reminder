# Server Configurations
server:
  port: 8000

# Application Configurations
app:
  host: http://localhost
  port: 8000
  auth:
    tokenSecret: 926D96C90030DD58429D2751AC1BDBBC
    tokenExpirationMs: 864000000 # 10 days
    freepass:
      enable: true
      masterToken: hst
  oauth2:
    authorizationFinalizeUrlTemplate: '${host}/login-success?token=${token}&memberId=${memberId}'
    reminderServiceFE: ${app.host}:8080
    # 인가된 redirect url
    authorizedRedirectUris:
      - ${app.host}:${app.port}/oauth2/finalize-authorization

# Framework Configurations
spring:
  profiles:
    active: local
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://database-1.clozyxtm7nwy.ap-northeast-2.rds.amazonaws.com:3306/reminder_db
    username: hst
    password: hstqweQWE
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: false
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: dlgusrb0808@gmail.com
    password: cjsrn1992!
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true